{
  "file": "packages/coding-agent/src/cli/credential-print.ts",
  "description": "pi 汉化词表：packages/coding-agent/src/cli/credential-print.ts",
  "replacements": {
    "Unknown provider \"${cliProvider}\". Use --list-models to see available providers.": "未知提供商 \"${cliProvider}\"。使用 --list-models 查看可用提供商。",
    "Unable to resolve the requested provider/model": "无法解析所请求的提供商/模型",
    "Using custom model id": "使用自定义模型 ID",
    "Model \"${cliModel}\" not found. Use --list-models to see available models.": "找不到模型 \"${cliModel}\"。使用 --list-models 查看可用模型。",
    "Provider \"${providerId}\" is configured with OAuth, not an API key": "提供商 \"${providerId}\" 配置的是 OAuth，而非 API 密钥",
    "Provider \"${providerId}\" is not configured with an OAuth bearer token": "提供商 \"${providerId}\" 未配置 OAuth Bearer 令牌",
    "No usable ${kind === \"api_key\" ? \"API key\" : \"OAuth bearer token\"} is configured": "未配置可用的 ${kind === \"api_key\" ? \"API 密钥\" : \"OAuth Bearer 令牌\"}",
    "Multiple configured providers matched (${credentials.map(({ providerId }) => providerId).join(\", \")}). Specify --provider.": "多个已配置提供商匹配（${credentials.map(({ providerId }) => providerId).join(\", \")}）。请指定 --provider。"
  }
}
